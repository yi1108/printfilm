# 发布记录 · 2026-08-17

## 2026-08-17 19:05 · 分集生成 400：保存后旧分镜 id（热修）

- **操作者**：Agent（按用户要求）
- **方式**：SSH 热修 backend + 重启 api
- **范围**：backend / api
- **变更摘要**：
  - `POST /episodes/{id}/fragments` 保存后显式重查分镜 id 再返回（`expire_on_commit=False` 会话会把已删除的旧 id 返回给前端）
  - 生成接口对不上 id 时打日志，提示「保存后分镜已更新」
- **验证**：
  - [x] `test_match_fragments_generate.py` 通过
  - [x] `ai-movie-api` 已重启
- **备注 / 回滚**：
  - 复现：保存分镜后立刻 `POST .../generate`，body 带着旧 fragment_ids → 400「没有可生成的分镜」
  - 请再点一次「生成」即可

## 2026-08-17 18:48 · 科普中间文件不再上传 OSS（热修）

- **操作者**：Agent（按用户要求）
- **方式**：SSH 热修 backend + 重启 api；OSS 队列积压已清空
- **范围**：backend / api
- **变更摘要**：
  - 科普流水线只把成片 `final.mp4` 入 OSS 队列
  - 分镜图、配音、镜头视频、尾帧只落本地 `/static`，供 ffmpeg 合成
  - 管理端「补传本地媒体」不再扫描分镜中间文件，避免再次灌满 oss 队列
- **验证**：
  - [x] 单测 `test_kepu_oss_final_only.py` 通过
  - [x] `ai-movie-api` active，`/api/health` ok
  - [x] Redis `oss` 队列已 `DEL`（pending 归零）
  - [ ] worker 待 111 出视频结束后再重启，之后新任务才走新逻辑
- **备注 / 回滚**：
  - 111 当时仍在 `VIDEOING`，未重启 worker，以免打断 Seedance
  - 回滚：还原 `storage.py` / `pipeline.py` / `oss_queue.py` 后重启 api+worker

## 2026-08-17 17:30 · 当日汇总全量发布

- **操作者**：Agent（按用户要求）
- **方式**：`deploy_kepu.py` 全量
- **范围**：frontend / admin / api / worker / nginx / env
- **变更摘要**（含当日已热修项，一并打包上线）：
  - **漫剧**：资产已出图/上传 UX；项目资产库分页；分集 `seed_from_script` MissingGreenlet 修复；LLM JSON 解析加固；摘要 generating 超时重入队
  - **队列**：Celery 独立 `drama` 队列；管理后台队列监控 `/queues`；Worker 动态管理 API
  - **API 性能**：admin 队列 inspect 轻量/缓存，避免阻塞主 API
  - **用户端**：定价/钱包/充值记录、账号资料编辑、API Key、法务页等
  - **成片**：ffmpeg 字幕烧录、Seedance 分镜约束等
- **验证**：
  - [x] `ai-movie-api` / `ai-movie-worker` active
  - [x] `https://kepu.printfilm.com/api/health` ok
  - [x] `https://www.printfilm.com/` HTTP 200
  - [x] `https://admin.printfilm.com/` HTTP 200
  - [x] 队列积压 `by_name` 均为 0
- **备注 / 回滚**：
  - 全量脚本已覆盖远端 `backend/.env` 模板；若 EPAY/OSS 回调有手工改动请核对
  - 回滚可用 `/opt/ai_movie.bak`（若存在）或 git 版本 + 重跑 deploy

## 2026-08-17 17:22 · 分集切分镜 500 + 资产库分页（热修）

- **操作者**：Agent（按用户要求）
- **方式**：SSH 热修 backend seed + 前台 build + 重启 api
- **范围**：backend / frontend
- **变更摘要**：
  - 修复 `POST /api/drama/episodes/seed_from_script` 新建分集时 `MissingGreenlet`（async 下 lazy load `episode.fragments`）
  - 项目工作流资产库增加分页（12/24/36 条每页）
- **验证**：
  - [x] 线上 project 12 调用 `seed_episodes_from_script` 成功：5 集 / 68 分镜
  - [x] `ai-movie-api` active，health ok
- **备注 / 回滚**：
  - 根因见 `seed.py` `_list_episode_fragments` 显式查询替代 relationship lazy load

## 2026-08-17 17:14 · 资产已出图/上传 UX（热修）

- **操作者**：Agent（按用户要求）
- **方式**：SSH 热修 4 文件 + 前台 build + 重启 api
- **范围**：frontend / api
- **变更摘要**：
  - 已有图（AI 生成或本地上传）视为已出图，按钮改为「重新生成形象」
  - 「一键生成未出图」计数排除已有图资产
  - 资产详情弹窗支持上传/更换图片
  - 上传接口写入 `generation.status=done`，避免仍显示生成中
- **验证**：
  - [x] 服务器 `frontend` build 成功
  - [x] `ai-movie-api` / `ai-movie-worker` active
  - [x] `https://www.printfilm.com/` HTTP 200
  - [x] `/api/health` ok
- **备注 / 回滚**：
  - 仅热修相关文件；回滚需还原 4 个文件并 rebuild 前台

## 2026-08-17 15:05 · Celery 队列拆分（热修）

- **操作者**：Agent（按用户要求）
- **方式**：SSH 热修 api/worker + systemd 单元
- **范围**：backend Celery / worker
- **变更摘要**：
  - 新增 `drama` 队列：漫剧 LLM（摘要/分集/分镜）与资产生图，不再与科普 `pipeline` 共用
  - `video`：所有视频生成；`oss`：异步上传；`pipeline`：仅科普/Studio 多阶段成片
  - Worker 监听 `-Q drama,oss,video,pipeline`（drama 优先）
  - 管理后台「队列任务」页：`/queues`，API `GET /api/admin/queues`
  - `/api/health` 返回各队列 `by_name` 积压
- **验证**：
  - [x] `ai-movie-api` / `ai-movie-worker` active
  - [x] health 显示 `by_name: {drama:0, oss:159, video:1, pipeline:6}`
- **备注 / 回滚**：
  - 旧任务若仍在 `pipeline` 队列会照常消费；新漫剧任务走 `drama`

## 2026-08-17 14:26 · 账号资料可编辑（热修）

- **操作者**：Agent（按用户要求）
- **方式**：SSH 热修 api + 前台 build；未跑全量
- **范围**：frontend / api
- **变更摘要**：
  - 账号信息：用户名、邮箱、手机号可编辑保存
  - 手机号只记录联系方式，不发验证码
- **验证**：
  - [x] 前台 `npm run build` 成功
  - [x] `ai-movie-api` active
  - [x] `PATCH /api/auth/me` 已注册，`phone` 字段可读
- **备注 / 回滚**：
  - 新增 `users.phone` 列；回滚代码后列可保留

## 2026-08-17 13:55 · 清空线上漫剧并全量同步本地（含画布）

- **操作者**：Agent（按用户要求）
- **方式**：SSH 热修数据；`import_drama_sqlite_to_postgres.py --replace`
- **范围**：Postgres `drama_*` 表
- **变更摘要**：
  - 删除线上全部漫剧项目/资产/分集/分镜
  - 按本地 sqlite 主键原样写入（项目 id 2 / 7 / 8），画布 `canvas_nodes.assetId` 可对齐
- **验证**：
  - [x] 3 项目 / 197 资产 / 12 集 / 142 分镜 / 728 分镜资产引用
  - [x] 大禹治水 91 个画布节点 assetId 全部存在
  - [x] 自由测试 5 节点 + 5 边
- **备注 / 回滚**：
  - 旧线上漫剧行已删除，无法从库内回滚；媒体仍在 OSS
  - 用量 `usage_events.drama_project_id` 已置空，未改账本金额

## 2026-08-17 13:42 · 绑定 www / admin.printfilm.com（热修）

- **操作者**：Agent（按用户要求）
- **方式**：SSH 热修 nginx + certbot + CORS；未跑全量 `deploy_kepu.py`
- **范围**：nginx / env / oss CORS
- **变更摘要**：
  - 禁用旧站 `printfilm.conf`（原反代 7050/7051 的 ai-manju）
  - `www.printfilm.com` / `printfilm.com` 改为 PRINTFILM 前台 dist（含 `/api`、`/static`、`/epay/notify`）
  - `admin.printfilm.com` → 管理后台 dist
  - `CORS_ORIGINS` 与 OSS 浏览器 CORS 增加上述 HTTPS 源
  - 发布脚本会持续禁用 `printfilm.conf`，避免下次全量抢回域名
- **验证**：
  - [x] `https://www.printfilm.com/` HTTPS 200
  - [x] `https://www.printfilm.com/api/health` ok
  - [x] `https://admin.printfilm.com/` HTTPS 200
  - [x] `https://admin.printfilm.com/api/health` ok
  - [x] 原 `kepu.printfilm.com` / `admin.kepu.printfilm.com` 保持不变
- **备注 / 回滚**：
  - 旧配置仍在 `/etc/nginx/sites-available/printfilm.conf`；恢复软链并去掉新 www 站点即可回滚
  - 易支付回调仍走 `https://kepu.printfilm.com/epay/notify`

## 2026-08-17 12:04 · 字幕合成修复 + 分镜台清理（全量）

- **操作者**：Agent（按用户要求）
- **方式**：`deploy_kepu.py` 全量（首次因 frontend TS 类型错误中断，修复后重跑成功）
- **范围**：frontend / admin / api / worker / nginx / env
- **变更摘要**：
  - 成片字幕：合成阶段始终用 ffmpeg 烧录口播字幕；`split` 布局不再跳过底部字幕（标题叠字留在顶部）
  - 分镜工作台：去掉「结构摘要」旁错误的「示例聚合」占位，以及无实际作用的「分镜设置」假控件
  - 顺带修前端构建：`composeEpisodeVideoClient` BlobPart 类型、`CanvasStore` 重复 `kind === 'video'` 判断
- **验证**：
  - [x] `https://kepu.printfilm.com/api/health` ok
  - [x] 前台首页 HTTPS 200（`index-3tb1K94l.js` / `index-s_7zELqT.css`）
  - [x] `https://admin.kepu.printfilm.com/` HTTPS 200
  - [x] `https://admin.kepu.printfilm.com/api/health` ok
  - [x] api / worker active
  - [ ] 业务冒烟：开源展示模板成片底部口播字幕、分镜台无「示例聚合」
- **备注 / 回滚**：
  - 全量脚本覆盖远端 `backend/.env`；EPAY/CORS/OSS 与脚本模板一致
  - Celery worker 已重启，监听 `pipeline,oss`
  - 旧成片需在工作室点「拼接成片」或重新合成后才会带新字幕逻辑
