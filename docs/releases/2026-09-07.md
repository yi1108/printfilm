## 2026-09-07 · 分镜重试上限修正 + Seedream CG 策略重试

- **操作者**：Agent
- **方式**：全量发布（`deploy_kepu_8136.py`）
- **范围**：api / web
- **变更摘要**：
  - 分镜 `generation_attempts` 3 次上限改为只约束同一次任务内自动重试；用户点生成 / 任务重试会清零计数，不再误拦
  - 超限失败文案幂等拼接，保留真实 `root_error`（如参考图 PrivacyInformation），避免「上限：root：root」
  - Seedream 文案/输出策略命中时追加 CG 厚涂风格重试；参考图 `PrivacyInformation` / `InputImageSensitive` 不空跑
  - 前端错误提示同步说明：超限不是禁止再点生成
- **验证**：
  - [x] `https://www.printfilm.com/api/health`（`ok:true`，`task_runtime.healthy`，scheduler/poller/watchdog running）
  - [x] 前台首页 200（`www.printfilm.com`）
  - [x] admin 首页 200（`admin.printfilm.com`）
- **备注 / 回滚**：回滚下列文件后重启 API，并重发前端 dist：
  `episodes.py` / `ark.py` / `generation.py` / `jobs.py` / `executor.py` / `service.py` / `dramaGenError.ts`
  提交：`2c09e4f`

---

## 2026-09-07 · 科普分阶段计费（确认分镜不再一次预扣全片）

- **操作者**：Agent
- **方式**：全量发布（`deploy_kepu_8136.py`）
- **范围**：api / web
- **变更摘要**：
  - 科普 `project_pipeline` 按 `script` / `assets` / `videos` 分段预扣与执行；确认分镜只冻出图+配音
  - pipeline 严格按 `payload.phase` 执行，避免「冻 A 跑 B」；成片走 `project_compose_only`
  - 计费与 pipeline 共用 `kepu_stages` 旁白就绪判定（整片旁白文件或镜头文件）
- **验证**：
  - [x] `https://www.printfilm.com/api/health`（`ok:true`，task_runtime healthy）
  - [x] 前台 / admin 首页 200
  - [x] 本地 `pytest tests/test_kepu_phase_billing.py`（9 passed）
- **备注 / 回滚**：回滚 `kepu_stages.py` / `estimates.py` / `pipeline.py` / `handlers.py` / `projects.py` / `StoryboardPage.tsx` 后重启 API 并重发前端 dist
  提交：`6905184`