# 发布记录 · 2026-08-25

## 2026-08-25 18:29 · 杭州 8.136.41.61 全量

- **操作者**：Agent（参考音频过短自动重生成 + 用户要求部署）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 音色合成默认更长试听句；落盘强制 ≥2s（补足）/ ≤15s（截断）
  - 分镜视频提交前检测过短角色/旁白参考音，自动重新合成或补足
  - Seedance `audio duration` 错误中文化（参考音频过短，非图片）
- **验证**：
  - [x] 脚本 `DONE`（约 1.1 min）；`ai-movie-api` active；health 200；poller running
  - [x] Host `kepu.printtfilm.com` / `admin.kepu.printtfilm.com` 首页 200（`Last-Modified: Tue, 25 Aug 2026 10:28 GMT`）
  - [ ] 业务：片段 12 等因参考音频过短失败的分镜重新生成应可通过
- **备注 / 回滚**：
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-25 18:16 · 杭州 8.136.41.61 全量

- **操作者**：Agent（修复分镜视频自动取消 + 用户要求部署）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 入队生成时清除进程内「本集已取消」标记，避免新任务被立刻作废
  - 取消态展示真实任务/分镜错误，不再一律只显示「已取消」
  - Worker 收到 `cancelled` 结果时正确标记任务终态
- **验证**：
  - [x] 脚本 `DONE`（约 1.4 min）；`ai-movie-api` active；health 200；poller running
  - [x] Host `kepu.printtfilm.com` / `admin.kepu.printtfilm.com` 首页 200（`Last-Modified: Tue, 25 Aug 2026 10:15 GMT`）
  - [ ] 业务：分集 115 片段 01 重新生成应能正常入队，不再秒变「已取消」
- **备注 / 回滚**：
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-25 17:26 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求全量发布）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 修复「AI 重新分镜」未清空旧分镜：`force=true` 时正确传入任务并 `preserve_protected=False`，覆盖已有视频/手改分镜
- **验证**：
  - [x] 脚本 `DONE`（约 3.1 min）；`ai-movie-api` active；health 200；poller running
  - [x] Host `kepu.printtfilm.com` / `admin.kepu.printtfilm.com` 首页 200（`Last-Modified: Tue, 25 Aug 2026 09:23 GMT`）
- **备注 / 回滚**：
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 线上再点「AI 重新分镜」应整集替换，不再追加在旧镜后面

## 2026-08-25 10:43 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求全量发布）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - AI 重新分镜弹窗：去掉 Cursor SKILL.md 上传说明提示
  - Skill 列表支持预览全文、下载 `.md`
- **验证**：
  - [x] 脚本 `DONE`（约 1.4 min）；`ai-movie-api` active；health 200
  - [x] Host `kepu.printtfilm.com` / `admin.kepu.printtfilm.com` 首页 200
- **备注 / 回滚**：
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 强刷后打开「AI 重新分镜」可见预览/下载按钮
