# 发布记录 · 2026-08-11

## 2026-08-11 ~13:55 · 全量发布到线上机器

- **操作者**：Agent（按用户要求）
- **方式**：`deploy_kepu.py` 全量（中途因 Windows 控制台编码中断后，补齐重启与健康检查）
- **范围**：源码同步、frontend build、api/worker、nginx（kepu）
- **变更摘要**：
  - 前台/后台隐藏漫剧菜单入口
  - 科普分镜时长约束、合成 `shot_id` 修复等此前已合入代码上线
  - 明确：**不以 OSS 上传 SPA 作为发布**；nginx 托管 `/opt/ai_movie/frontend/dist`
- **验证**：
  - [x] `https://kepu.printfilm.com/` 200
  - [x] `https://kepu.printfilm.com/api/health` ok（修复启动卡顿后）
- **备注 / 回滚**：
  - 启动时模板封面同步 OSS 曾卡住 API；`seed_templates` 改为库内已有 https 封面则跳过重传
  - API 临时改为 `--workers 1`，降低 `create_all` 竞态

## 2026-08-11 ~14:06 · 管理后台绑定二级域名

- **操作者**：Agent
- **方式**：SSH 热修（构建 admin + nginx + certbot）
- **范围**：admin build、nginx `admin.kepu.printfilm.com`、CORS、TLS
- **变更摘要**：
  - 发布 https://admin.kepu.printfilm.com/
  - Let’s Encrypt 证书签发
  - `CORS_ORIGINS` 增加 admin 域名
  - `deploy_kepu.py` 纳入 admin 构建与站点配置（后续全量可用）
- **验证**：
  - [x] admin 首页 HTTPS 200
  - [x] `https://admin.kepu.printfilm.com/api/health` ok
- **备注 / 回滚**：无

## 2026-08-11 ~14:08 · 易支付配置 + demo 充值

- **操作者**：Agent
- **方式**：SSH 改 `.env` + admin API 调账
- **范围**：env（EPAY_*）、用户余额
- **变更摘要**：
  - 写入与开发环境一致的 `EPAY_PID` / `EPAY_KEY`（密钥不记入本文）
  - 回调先设为带 `/api/` 的 notify（随后因 WAF 修正，见下条）
  - `demo@example.com` 余额设为 ¥2000
- **验证**：
  - [x] 进程加载到 `EPAY_PID`
  - [x] demo `balance_fen=200000`
- **备注 / 回滚**：无

## 2026-08-11 ~14:12 · 修复易支付「返回异常」（WAF）

- **操作者**：Agent
- **方式**：SSH 热修 nginx + env + `epay.py`
- **范围**：nginx、`EPAY_NOTIFY_URL`、后端 epay 客户端
- **变更摘要**：
  - 根因：`pay.gitcc.com` 防火墙拦截 `notify_url` 中含 `/api/` 的请求
  - 回调改为 `https://kepu.printfilm.com/epay/notify`
  - nginx `location = /epay/notify` → `8000/api/billing/epay/notify`
- **验证**：
  - [x] `topup_10` 支付宝下单返回 `trade_no` + `qr_payload`
- **备注 / 回滚**：旧 notify 路径勿再写回含 `/api/` 的 URL

## 2026-08-11 ~14:15 · 定价套餐金额 ×10

- **操作者**：Agent
- **方式**：SSH 热修 `billing.py` + 重启 api
- **范围**：api（SKU）
- **变更摘要**：
  - 体验 ¥100 / 基础 ¥490 / 进阶 ¥990（到账 ¥1040）/ 专业 ¥1990（到账 ¥2200）
- **验证**：
  - [x] `GET /api/billing/skus` 金额已更新
- **备注 / 回滚**：改回 `backend/app/services/billing.py` 中 `SKUS` 原分值即可
