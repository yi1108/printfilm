# 发布记录 · 2026-09-01

## 2026-09-01 11:08 · 管理端仪表盘修复 + 视频渠道 UI；杭州全量

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 管理端仪表盘：日趋势按方言分桶（Postgres `CAST AS DATE` / SQLite `date()`），避免生产 `/api/admin/stats` 500；展示上游成本；「用量明细」链到 `/orders?tab=usage`
  - 订单页支持 URL `tab`；科普/漫剧详情用量含 TTS，任务列区分已扣/预估
  - 模型路由：快捷「添加视频渠道」等预设、能力徽标、缺视频渠道提示
  - 补充 `tests/test_admin_stats.py`
- **验证**：
  - [x] 脚本 `DONE`（约 1.1 min）；`ai-movie-api` active；`/api/health` 200
  - [x] 前台 / admin 静态首页 200
  - [x] health 中 video 模型为 `doubao-seedance-2-5-260628`
- **备注**：强刷 admin.printfilm.com → 设置 → 模型路由，可用「添加视频渠道」填写方舟 Key
