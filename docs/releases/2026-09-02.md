# 发布记录 · 2026-09-02

## 2026-09-02 11:39 · 管理端仪表盘/财务 + 统计修复；杭州全量

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 管理端：财务列表 `/finance`；仪表盘改版（周期 KPI、领域/用户排行柱状图、布局收紧）；用户管理调用统计修复
  - 后端：`/api/admin/stats` days 参数 int 归一化；能力分布按 `billing_key` 推导；财务日汇总 API
  - 用户端：修复 `BillingAlertHost` 构建导入路径
- **验证**：
  - [x] 脚本 `DONE`（约 0.9 min）；`ai-movie-api` active；`/api/health` 200；poller running
  - [x] 前台 / admin 静态首页 200（`Last-Modified: Wed, 02 Sep 2026 03:39 GMT`）
- **备注**：强刷 `admin.printfilm.com` / `www.printfilm.com` 后验收仪表盘与财务列表

## 2026-09-02 17:10 · Code review 修复；杭州全量

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 用户排行支持 cost/calls 指标；能力分桶与筛选统一；非法 days 返回 422
  - 运维 Tab 独立 30 日全量数据；UsersPage 统计不再误用分页 total
  - 财务同步错误处理、合计毛利率；登录页 Firefox 统一校验提示
- **验证**：
  - [x] 脚本 `DONE`（约 1.0 min）；`ai-movie-api` active；`/api/health` 200
  - [x] 前台 / admin 静态首页 200（`Last-Modified: Wed, 02 Sep 2026 09:10 GMT`）
