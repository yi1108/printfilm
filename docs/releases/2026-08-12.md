# 发布记录 · 2026-08-12

## 2026-08-12 16:13 · 漫剧分镜/分集全量发布

- **操作者**：Agent（按用户要求）
- **方式**：`deploy_kepu.py` 全量（首次因 frontend TS 类型错误中断，修复后重跑成功）
- **范围**：frontend / admin / api / worker / nginx / env
- **变更摘要**：
  - 人物介绍：规则推断 + LLM 批量补齐 stub 角色小传（鲧/舜等）
  - Seedance 漫剧模式：画面行不配音，仅对白/旁白段配音+字幕；修正字幕 cue 误读旁白
  - 分镜生成互斥：仅拦截同一分镜重复提交，其他镜可编辑与单独生成
  - 取消视频任务 API + 队列面板停止按钮
  - 分集列表 UI 重做（海报卡片、进度、状态）
  - 分镜故事板画布（episodeCanvas）、分集视频时间轴播放器
  - LLM 分镜规划（fragment_plan）与单集重新分镜
- **验证**：
  - [x] `https://kepu.printfilm.com/api/health` ok
  - [x] 前台首页 HTTPS 200
  - [x] `https://admin.kepu.printfilm.com/api/health` ok
  - [x] admin 首页 HTTPS 200
  - [ ] 业务冒烟：重新分镜人物介绍、画面不念旁白、分镜并行生成、分集列表 UI
- **备注 / 回滚**：
  - 全量脚本覆盖远端 `backend/.env`；EPAY/CORS/OSS 与脚本模板一致
  - Celery worker 已重启，监听 `pipeline,oss`

## 2026-08-12 16:29 · UI 打磨 + 全局弹窗 + 本地同步线上

- **操作者**：Agent（按用户要求）
- **方式**：`deploy_kepu.py` 全量
- **范围**：frontend / admin / api / worker / nginx
- **变更摘要**：
  - 漫剧 Agent 首页 UI 重做（hero、项目列表行卡片、字数计数）
  - 剧情大纲 / 资产库 / 分集视频三步工作流页头统一
  - 全局 `DialogHost` 升级（Lucide 图标、lime 主题）
  - 全部生成确认改用 `dialog.confirm`（移除 `window.confirm`）
  - 分镜单镜/全量生成、批量生图、重新生成分集剧本等确认弹窗
- **验证**：
  - [x] `https://kepu.printfilm.com/api/health` ok
  - [x] 前台首页 HTTPS 200（`index-BxUOEMu1.js` / `index-DuEAXr5P.css`）
  - [x] admin 首页 HTTPS 200
  - [x] api / worker active
  - [ ] 业务冒烟：生成确认弹窗、Agent 首页、三步工作流 UI
- **备注 / 回滚**：
  - 线上 `/opt/ai_movie` 已与本地源码包同步；数据库未清库
  - worker 已重启
