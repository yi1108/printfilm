# 发布记录 · 2026-08-31

## 2026-08-31 15:12 · 资产生图 TaskRun 未提交热修

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 热修
- **根因**：`/api/drama/generation/image|video` 入队 `commit=False` 后未 `db.commit()`，TaskRun 与资产状态随请求结束回滚；日志有 task_id 但库中 `asset_image` 恒为 0
- **修复**：入队成功后 `await db.commit()`；前端队列写入 `task_id` 便于详情跳转
- **验证**：
  - [x] API 已重启；health 200
  - [ ] 用户侧再触发 1 张生图后，管理端任务中心应出现 `asset_image` pending/进行中

## 2026-08-31 15:05 · 计费预检 + 生图排队态；杭州全量

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 批量/单张资产生图入队前余额预检（累计排队占用 + `GET /api/billing/preflight`）；不足 402 + 前端「去充值」
  - 生图状态：`queued`（已入队）→ Worker 执行时 `generating`；前端最多 3 路提交、队列面板区分排队/生成中
  - 任务创建写入 `billing_estimate_fen`，避免并行入队透支
  - 预扣失败时同步资产 `generation.failed`
- **验证**：
  - [x] 脚本 `DONE`（约 1.0 min）；`ai-movie-api` active；`/api/health` 200
  - [x] 前台 / admin 静态首页 200
- **备注**：强刷 www.printfilm.com 后验证资产库批量生图余额不足拦截与排队文案

## 2026-08-31 11:05 · 分镜 LLM 空响应加固；杭州全量

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：api（`ark.py` / `llm_client.py`）
- **变更摘要**：
  - `chat_storyboard`：启用 `response_format: json_object`、空内容自动重试、复用 `_extract_json` 容错解析
  - `llm_client`：HTTP 200 但 body 为空时抛出明确错误
  - 修复项目 #255 等在 SCRIPTING 5% 因 LLM 返回空串导致 `Expecting value: line 1 column 1` 失败
- **验证**：
  - [x] 脚本 `DONE`（约 1.6 min）；`ai-movie-api` active；`/api/health` 200
  - [x] 前台 / admin 静态首页 200
- **备注**：可在 www.printfilm.com 对失败项目点「重新生成」重试分镜

## 2026-08-31 14:35 · 易支付回调改 www + 补账 yuanzhongqiao888

- **操作者**：Agent
- **方式**：SSH 热修 env + SQL 补账；frontend dist 热更
- **范围**：`EPAY_NOTIFY_URL` / `EPAY_RETURN_URL`、用户 #232 订单 `PF178815741302326b015830`
- **变更摘要**：
  - 根因：回调指向 `http://kepu.printtfilm.com/epay/notify`（错误域名 + HTTP），易支付侧已扣款但 nginx 无 notify 命中
  - 易支付查询确认 ¥100 已付；手动入账 +10000 分，订单改 `paid`
  - 回调/回跳改为 `https://www.printfilm.com/epay/notify` 与 `/pricing?paid=1`
  - 前端：API 402 全局弹窗 + 各页余额不足内联「去充值」链接
- **验证**：
  - [x] 用户余额 ¥100.20（含原 ¥0.20）
  - [x] `EPAY_NOTIFY_URL` 已写入远端 `.env`，api 已 restart
  - [x] frontend `npm run build` 通过
- **备注**：后续全量发布需沿用 deploy 脚本中 www 回调配置，勿再写 kepu.printtfilm

## 2026-08-31 14:55 · 清理 8/27 僵尸分镜视频排队任务

- **操作者**：Agent
- **方式**：SSH + SQL 批量作废
- **范围**：TaskRun #1440–#1450（用户 123456@qq.com，分集 #320 串行批次）
- **变更摘要**：分镜侧已取消但 TaskRun 仍 pending 的 11 条僵尸任务全部标记为 `cancelled`
- **验证**：
  - [x] #1440–#1450 均为 `cancelled`
  - [x] 8/27 前创建的 `fragment_video` pending 余量为 0

## 2026-08-31 10:10 · 杭州 8.136.41.61 全量

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：frontend（含 admin 同包）/ api 重启
- **变更摘要**：
  - 风格配置页：竖屏模板预览按真实比例 `contain` 显示，卡片标注 `default_ratio`
  - 左栏缩略图 / 右栏实时预览跟随当前输出比例（含 `9:16`）
  - 三栏布局防横向溢出；选风格时同步模板默认画幅
- **验证**：
  - [x] 脚本 `DONE`（约 1.4 min）；`ai-movie-api` active；`/api/health` 200
  - [x] 前台 / admin 静态首页 200
- **备注**：强刷浏览器缓存后查看 `/studio/{id}/style`

## 2026-08-31 09:56 · 杭州 8.136.41.61 全量

- **操作者**：Agent（统一任务计费模块上线）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1` `SKIP_LOCAL_BUILD=1`）；SSH 密码改由 `deploy/.env.secrets.local` 注入
- **范围**：frontend / admin / api（计费包 + 管理端用量）
- **变更摘要**：
  - TaskRun 中心计费：预扣 → `record_line` → `settle_task`；轻量任务 / deferred 视频轮询结算
  - 入队前余额预检 402；freeze/settle 行锁；deferred 结算幂等门闩
  - 管理端：用量明细 Tab、任务详情计费、队列费用列
  - 修复 `pipeline.py` 注释缺 `#` 导致 SyntaxError（首次发布 API 起不来，二次热修后恢复）
- **验证**：
  - [x] 脚本 `DONE`；`ai-movie-api` active；`/api/health` 200（`ok:true`，poller running）
  - [x] 前台 / admin 静态首页 200（本机 Host 探测）
- **备注**：工作区计费改动尚未 git commit；密钥仅在本机 secrets，未写入脚本明文
