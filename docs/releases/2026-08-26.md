# 发布记录 · 2026-08-26

## 2026-08-26 18:56 · 杭州 8.136.41.61 全量

- **操作者**：Agent（通用 OpenAI 兼容文字模型 + DeepSeek）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）；发布后同步渠道 `openai-default`
- **范围**：frontend / admin / api / env（文字模型）
- **变更摘要**：
  - 渠道 models 变更后自动同步逻辑模型与默认文本模型（不再钉死 kimi）
  - 路由请求失败时回落同能力可用模型；`deepseek*`/`kimi*` 关闭 thinking
  - 线上文字上游改为 DeepSeek：`https://api.deepseek.com` + `deepseek-v4-pro`
- **验证**：
  - [x] 脚本 `DONE`；本机 `ai-movie-api` active；health `llm=deepseek-v4-pro`
  - [x] Host `kepu.printfilm.com` / `admin.kepu.printfilm.com` / `www.printfilm.com` 首页 200
  - [ ] 本机外网直连 HTTPS 曾 connection reset（以服务器侧 Host 探测为准）
- **备注**：commit `c9c3b0e`；不含短剧流程大改分支 `feature/long-script-input-steps`

## 2026-08-26 15:20 · 杭州 8.136.41.61 全量

- **操作者**：Agent（后期字幕 + 分镜版本历史）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 后期字幕：生成提示禁止 Seedance 画面烧录，保留口播，字幕板导出 SRT
  - 分镜历史版本：成片独立落盘并复制 hist，避免覆盖同路径导致只剩当前/v1
- **验证**：
  - [x] 脚本 `DONE`；health 200；首页 200（`Last-Modified: Wed, 26 Aug 2026 07:19 GMT`）
  - [x] admin 首页 200
- **备注**：不含 `feature/long-script-input-steps`（f80d5a5）；已丢中间版本无法找回

## 2026-08-26 11:01 · 杭州 8.136.41.61 全量

- **操作者**：Agent（取消关联被误认为删除资产）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**：
  - 分集编辑页去掉「删除资产」入口，避免与取消关联混淆
  - 已关联资产显示「取消关联」；Backspace 取消后提示仍可在「全集」查看
- **验证**：
  - [x] 脚本 `DONE`；health 200；首页 200（`Last-Modified: Wed, 26 Aug 2026 03:00 GMT`）
- **备注**：永久删除请走项目资产步骤页

## 2026-08-26 10:57 · 杭州 8.136.41.61 全量

- **操作者**：Agent（脚本关联 chip 无法 Delete + 资产删除入口）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**：
  - 分镜脚本 Backspace/Delete 可删除资产/时长关联标签
  - 去掉关联后同步 `asset_ids`
  - （已在 11:01 撤销）分集页永久删除入口
- **验证**：
  - [x] 脚本 `DONE`；health 200

## 2026-08-26 10:50 · 杭州 8.136.41.61 全量

- **操作者**：Agent（分集侧栏本集资产自定义加入）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**：
  - 角色、场景、道具增加「新建」与「导入」
- **验证**：
  - [x] 脚本 `DONE`；health 200
