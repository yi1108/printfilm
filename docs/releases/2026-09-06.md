## 2026-09-06 22:16 · OSS 上传误判修复 + Seedance 预扣抬高（热修）

- **操作者**：Agent
- **方式**：SSH 热修
- **范围**：api / env
- **变更摘要**：
  - P0：`is_local_static_url` 对 http(s) OSS URL 一律返回 False；`upload_local_url_sync` 成功判定改为须以 `http(s)://` 开头，避免本地 FFmpeg 副本导致「upload returned local URL」误抛、库字段不回写
  - P0：线上 `backfill_oss_media` 补传存量 `/static`（两轮合计 uploaded≈760 / failed=0；仍有少量 pending 多为缺本地文件 skipped）
  - P1：`fragment_video` 估时优先 `payload.duration_sec`，否则按分镜正文 `resolve_seedance_duration_from_content`；入队写入 `duration_sec`
  - P1：默认 `billing_est_seedance_tokens_per_sec` 20000→32000（并写入生产 `.env`）；`fragment_video` 预估加半张 seedream 余量
- **验证**：
  - [x] `https://www.printfilm.com/api/health`（`ok:true`，`task_runtime.healthy`，scheduler/poller/watchdog running）
  - [x] 线上断言：`tokens_per_sec=32000`，OSS https `is_local_static_url=False`
  - [x] journal 近 5–10min：`upload returned local URL` = 0
  - [x] `tests/test_oss_queue_backfill.py` + `tests/test_fragment_video_estimate.py` 6 passed
- **备注 / 回滚**：回滚 `storage.py` / `oss_queue.py` / `estimates.py` / `episodes.py` / `config.py` / `schemas_settings.py`，去掉 `.env` 中 `BILLING_EST_SEEDANCE_TOKENS_PER_SEC` 后 `systemctl restart ai-movie-api`

## 2026-09-06 21:25 · 任务调度看门狗 + 运行中孤儿恢复（热修）

- **操作者**：Agent
- **方式**：SSH 热修
- **范围**：api
- **变更摘要**：
  - 调度 / Selector 增加心跳；单次 tick 超时自动取消，避免卡死整环
  - 运行时看门狗：循环退出或心跳过期时软重启（不杀仍在跑的 job）
  - 运行中周期扫描僵死 `leased`/`running`（跳过本进程仍持有协程的任务）并重排队
  - `/api/health` 的 `ok` 与 `task_runtime.healthy` 绑定；暴露 `scheduler` / `poller` / `watchdog`
- **验证**：
  - [x] `https://www.printfilm.com/api/health`（`ok:true`，`task_runtime.healthy`，scheduler/poller/watchdog running）
  - [x] 本机 `curl 127.0.0.1:8000/api/health` 同上
  - [ ] 前台首页 200
  - [ ] （如有）admin 首页 200（`https://admin.printfilm.com/`）
  - [x] 单元测试 `tests/test_task_runtime_watchdog.py` 4 passed
- **备注 / 回滚**：回滚上述 7 个后端文件并 `systemctl restart ai-movie-api`

## 2026-09-06 21:30 · 分集剧本进度回写 + DeepSeek JSON + force 续跑（热修）

- **操作者**：Agent
- **方式**：SSH 热修
- **范围**：api
- **变更摘要**：
  - `#3331` 管理端进度一直 0%：`run_episode_scripts_job` 未回写 `task_runs.progress_percent`，现按 done/total 更新并写 `task.progress` 事件
  - DeepSeek `response_format=json_object` 要求 prompt 含 `json`：调用前自动注入，避免每集白打一轮
  - `force=true` 在 `episode_content_status=generating` 时跳过清空，进程重启可从已生成集续跑
- **验证**：
  - [x] `#3331` 重启后续跑 `4/12`，`progress_percent=33`
  - [x] journal：`force 续跑跳过清空 done=4/12`
  - [x] `tests/test_drama_llm_json.py` 8 passed
- **备注 / 回滚**：回滚 `llm.py` / `jobs.py` / `handlers.py` 并 `systemctl restart ai-movie-api`

## 2026-09-06 21:42 · 作废任务补 settle 解冻（热修）

- **操作者**：Agent
- **方式**：SSH 热修
- **范围**：api
- **变更摘要**：
  - `_mark_task_cancelled_stale` / 串行 batch 取消在 `billing_status=frozen` 时补 `settle_task`，避免预扣悬挂
  - 用户 `pengjh83@gmail.com`（#505）任务 `#3328`：API 重启 reconcile 标「分镜已生成完成」取消后未结算，冻结 ¥26.50；补结算后按真实 Seedance 用量 ¥35.84 结清（预扣 26.50 + 补差 9.34），`frozen_fen=0`
- **验证**：
  - [x] 全站终态 `frozen` 孤儿清零；用户 505 `frozen_fen=0` `balance_fen=5440`
  - [x] `tests/test_stale_pending_fragment_video.py` 含 settle 单测
- **备注 / 回滚**：回滚 `service.py` 并 `systemctl restart ai-movie-api`；账务勿回滚 ledger
