# 发布记录 · 2026-09-04

## 2026-09-04 19:10 · 支付宝无原生码则新开易支付站点；杭州热修

- **操作者**：Agent
- **方式**：SSH 热修（epay.py / illing.py + 上传 frontend dist）
- **范围**：api / frontend
- **变更摘要**：
  - 微信：pay_mode=qr，弹窗原生二维码
  - 支付宝：仅有收银台 payurl 时 pay_mode=redirect，自动新开易支付站点，弹窗等待到账并可「重新打开支付页」
- **验证**：
  - [x] wxpay → qr；alipay → redirect + payurl
  - [x] i-movie-api active；前台 dist 已替换
- **备注 / 回滚**：*.bak_redirect_* / rontend/dist.bak_redirect_*

## 2026-09-04 19:06 · 支付二维码不用易支付收银台链接；杭州热修

- **操作者**：Agent
- **方式**：SSH 热修（epay.py）
- **范围**：api
- **变更摘要**：
  - 根因：支付宝 mapi 只回 payurl=pay.gitcc.com/pay/submit/...，前端把它编成二维码，扫码打开易支付站点
  - 微信正常返回 weixin:// 原生码
  - 修复：mapi 强制 device=pc；仅用原生 qrcode/urlscheme/图片；无扫码内容时明确报错，提示开通扫码/当面付通道
- **验证**：
  - [x] wxpay → 原生 weixin://
  - [x] alipay → 清晰错误（待商户开通支付宝扫码通道）
- **备注 / 回滚**：epay.py.bak_qr_*；支付宝需在 pay.gitcc.com 开通扫码通道后恢复

## 2026-09-04 17:15 · QQ 邮箱 465 改 SMTP_SSL；杭州热修

- **操作者**：Agent
- **方式**：SSH 热修（上传 `email.py` + 重启 API）
- **范围**：api
- **变更摘要**：
  - 线上 SMTP 为 `smtp.qq.com:465`，原逻辑用明文 SMTP+STARTTLS 超时
  - 465 端口改为 `SMTP_SSL`；已向 `2856024429@qq.com` 重发重置邮件（约 774ms 成功）
- **验证**：
  - [x] `POST /api/auth/forgot-password` 200，无 `send email failed`
  - [x] `ai-movie-api` active
- **备注 / 回滚**：`email.py.bak_smtp465_*`

## 2026-09-04 17:03 · 任务执行器 + 管理端任务中心 + 找回密码；杭州全量

- **操作者**：Agent
- **方式**：`deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：frontend / admin / api（未改 nginx / 未动容器）
- **变更摘要**：
  - 执行器：`freeze` 前先读 `steps`；去掉会卸掉关系的 `db.refresh`；回归测试
  - 管理端任务中心：列表/详情展示提交参数与结果；实体链接防冒泡；空区块隐藏
  - 设置页保存上下文 / 上游模型目录；Seedream 预估默认 45k；DB 池模板保持 100+20
  - 用户端邮箱找回密码（forgot / reset）
- **验证**：
  - [x] `ai-movie-api` active；`db_pool.size=100` `max=120`；`poller=running`
  - [x] `https://www.printfilm.com/api/health` 200
  - [x] 前台首页 200（Host www）
  - [x] admin 首页 200（Host admin.printfilm.com）
- **备注 / 回滚**：commit `b1b8bd9`；线上 Seedream 预估仍以 `app_settings` overlay 为准

## 2026-09-04 16:27 · 任务卡在已租约 MissingGreenlet；杭州热修

- **操作者**：Agent
- **方式**：SSH 热修（上传 `executor.py` + `systemctl restart ai-movie-api`）
- **范围**：api
- **变更摘要**：
  - 根因：执行器先 `freeze_for_task`（`_lock_task` + `populate_existing`）再读 `task.steps`，触发异步懒加载 `MissingGreenlet`，协程死后状态停在 `leased`
  - 修复：在预扣前先取出 `steps[0]`；重启时 `_recover_orphaned_tasks` 回收 9 条孤儿租约
  - 备份：`/opt/ai_movie/backend/app/services/tasks/executor.py.bak_20260904_162736`
- **验证**：
  - [x] `ai-movie-api` active；journal `recovered orphaned task runs count=9`
  - [x] 原 #3100–#3117 九条由 `leased` → `running`，且出现 `task.started`
  - [x] `/api/health`：`scheduler_running_jobs=9`，`poller=running`；重启后无新的 `MissingGreenlet`
  - [x] `https://www.printfilm.com/api/health` 200
- **备注 / 回滚**：回滚可 `cp` 上述 `.bak_*` 后重启 API

## 2026-09-04 16:33 · DB 池调到 100 + 重试 QueuePool 失败任务；杭州热修

- **操作者**：Agent
- **方式**：SSH 热修（改 `.env` / PG `max_connections` + 应用内 `retry_task_for_user`）
- **范围**：api / postgres
- **变更摘要**：
  - `DB_POOL_SIZE=100`、`DB_MAX_OVERFLOW=20`（健康检查 `max=120`）；PG `max_connections=250`
  - 对 #3100/#3109/#3113/#3116/#3117（QueuePool 超时失败）自动重试为 #3118–#3122
  - 漫剧 #353/#354 刷新 `summary_status=generating`，前端轮询可继续等到结果
  - 发布脚本模板同步池配置，避免下次全量覆盖回 5+5
- **验证**：
  - [x] `/api/health` `db_pool.size=100` `max=120`；`ai-movie-api` active
  - [x] 重试任务已入队并进入 `running`
- **备注 / 回滚**：`.env.bak_pool_*`；PG 可用 `ALTER SYSTEM SET max_connections = 100` 后重启容器

## 2026-09-04 16:45 · 任务详情补齐提交参数/结果/资产关联；杭州 admin 热修

- **操作者**：Agent
- **方式**：SSH 热修（上传 `admin/dist`）
- **范围**：admin
- **变更摘要**：
  - 详情概览：有值才展示关联字段；补资产/剧本链接；提交参数可读摘要 + JSON；结果区块固定展示
  - 任务列表增加「提交参数」「结果」摘要列
- **验证**：
  - [x] 本地 `npm run build` 通过；远端 `admin/dist` 已替换
- **备注 / 回滚**：`/opt/ai_movie/admin/dist.bak_*`
