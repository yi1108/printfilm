# 发布记录 · 2026-08-21

## 2026-08-21 18:26 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户反馈：FFmpeg signal 15 应自动重试）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**：
  - 识别 FFmpeg SIGTERM（`Exiting normally, received signal 15` / returncode -15）
  - 成片合成自动重试最多 3 次；失败时错误改为短中文，不再堆整段 x264 日志
- **验证**：
  - [x] 脚本 `DONE`；health 200
- **备注 / 回滚**：已失败项目需点「重新拼接」；回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-21 17:38 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**：
  - 失败原因优先从同镜历史任务挖出真实根因（如 PrivacyInformation / content[n]），不再只显示「重试超过上限」
  - Seedance 审核失败会带上 content 槽位对应资产名（角色/场景「xxx」）；generation 持久化 `root_error`
  - 片段 02（#7375）已回填历史真人审核根因便于立即查看
- **验证**：
  - [x] 脚本 `DONE`；health 200；poller running
  - [x] 前台 `Last-Modified: Fri, 21 Aug 2026 09:37`
- **备注 / 回滚**：回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-21 17:27 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求发布线上）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 分集编辑左侧资产可点开详情：编辑提示词 / 重新生成形象 / 上传换图
  - 失败详情只展示错误原因（不再堆队列状态与平台任务元数据）；优先解析参考图审核等根因
  - 底部分镜失败态显示感叹号，可点开查看该镜失败原因
  - 分镜重试超限时附带上一轮真实错误文案
- **验证**：
  - [x] 脚本 `DONE`；`ai-movie-api` active；health 200；poller running
  - [x] 前台 / admin 本机 Host 200（`Last-Modified: Fri, 21 Aug 2026 09:26`）
- **备注 / 回滚**：回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-21 17:16 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**：
  - 生成队列错误中文化 + 处理建议（真人审核、衔接失败、分镜变更等）
  - 队列项可点击查看任务详情（平台状态/进度/事件/原始错误）
  - Seedance 创建失败后端写入可读中文；generate_status 附带近期终态任务
- **验证**：
  - [x] 脚本 `DONE`；`ai-movie-api` active；health 200；poller running
  - [x] 前台 / admin 本机 Host 200（`Last-Modified: Fri, 21 Aug 2026 09:15`）
- **备注 / 回滚**：回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-21 17:00 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求发布线上）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 分镜保存按 id upsert，避免每次生成前重建 id 导致「分镜上下文丢失」
  - 删除/重切分镜时作废引用旧 id 的在途 `fragment_video`（含 awaiting_poll）
  - 旧任务禁止重试；轮询文案改为「分镜已变更，请重新生成」
- **验证**：
  - [x] 脚本 `DONE`；`ai-movie-api` active；health 200；poller running
  - [x] 前台 / admin 本机 Host 探测 200（`Last-Modified: Fri, 21 Aug 2026 08:59`）
- **备注 / 回滚**：回滚上一包并 `systemctl restart ai-movie-api`；分集#113 请用当前分镜重新生成，勿重试旧失败任务

## 2026-08-21 11:06 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求发布线上）
- **方式**：`deploy_kepu_8136.py` 全量（首次上传后 SSH 断连，`SKIP_LOCAL_BUILD=1` 重试成功）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 登录页不再预填演示账号；去掉多余说明文案
  - 管理端登录同步去掉演示提示与冗余说明
- **验证**：
  - [x] 脚本 `DONE`；`ai-movie-api` active；health 200；poller running
  - [x] 前台 / admin 本机 Host 探测 200（`Last-Modified: Fri, 21 Aug 2026 03:05`）
- **备注 / 回滚**：回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-21 11:01 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求发布线上）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 剧情大纲「项目风格」改为卡片选择
  - 资产 seed：声音类名称不再当角色；去掉素材（material）入库与 UI
  - 设置页修改密码（`POST /api/auth/change-password`）
  - 分镜/生成相关既有改动一并上线
- **验证**：
  - [x] 脚本 `DONE`；`ai-movie-api` active；本机 health 200；poller running
  - [x] 前台 / admin 本机 Host 探测 200（`Last-Modified: Fri, 21 Aug 2026 03:00`）
- **备注 / 回滚**：回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-21 09:40 · 发布流程：默认跳过 nginx/certbot

- **操作者**：Agent（按用户要求）
- **变更**：`deploy_kepu_8136.py` 常规发布不再写 vhost、不再 certbot；需改站点时 `SETUP_NGINX=1`，需签/续证书时 `SETUP_TLS=1`。文档见 `docs/DEPLOY.md`、`deploy/README.md`。

## 2026-08-21 09:35 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求发布线上）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**（含昨日未单独成包的本地改动）：
  - 重新生成不被「已有成片」误取消；分镜视频历史版本
  - 资产 seed 防并发重复 + 同名合并；AI 分镜关联道具/素材
  - 科普字幕 CJK 字体查找与部署字体说明
- **验证**：
  - [x] 脚本 `DONE`；`ai-movie-api` active；health 200；poller running
  - [x] 前台 / 管理端首页 200（`Last-Modified: Fri, 21 Aug 2026 01:35`）
- **备注 / 回滚**：回滚上一包并 `systemctl restart ai-movie-api`
