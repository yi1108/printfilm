# 发布记录 · 2026-08-20

## 2026-08-20 19:40 · 杭州 8.136.41.61 全量

- **操作者**：Agent（角色重复 + 分镜未挂道具/素材）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**：
  - 资产 seed：行锁防并发；同名合并（保留有图/音色的）；列表进入时自动合并已有重复
  - 前端空库 auto-seed 加进程内防抖
  - AI 分镜：catalog/归一化/规则切分纳入道具与素材；补图类型含 material
- **验证**：
  - [x] 本机脚本 `DONE`；`ai-movie-api` active；health 200
  - [x] 单测 fragment_plan + asset_dedupe 8 passed
- **备注 / 回滚**：
  - 打开已有重复角色的项目资产页会自动合并同名卡（优先保留有图/音色）
  - 道具/素材关联需**重新跑 AI 分镜**后才会写入
  - 回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-20 19:24 · 杭州 8.136.41.61 全量

- **操作者**：Agent（线上重新生成被误取消）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**：
  - 修复 `reconcile_stale_pending_tasks`：分镜已有成片但 `generation` 为 queued/running/generating 时不再当成「跳过重复」取消（重新生成可正常入队）
- **验证**：
  - [x] 本机脚本 `DONE`；`ai-movie-api` active；health 200；poller running
  - [x] 单测 `test_stale_pending_fragment_video.py` 4 passed
- **备注 / 回滚**：回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-20 18:33 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api / nginx / env / 中文字体
- **变更摘要**：
  - 科普成片字幕方框：线上安装文泉驿字体；`FRAMECUT_FONT` 写入 `.env`；`ffmpeg_compose` 扩展 CJK 字体查找
  - 漫剧分镜：重新生成状态修复、入队徽标、视频历史版本（`params.video_versions`）
  - 部署脚本 bootstrap 增加 `fonts-wqy-*`，避免下次发布丢字体配置
- **验证**：
  - [x] 本机脚本结束 `DONE`；`ai-movie-api` active；PG/Redis healthy
  - [x] `127.0.0.1:8000/api/health` 与 Host 前台 `/api/health` 200
  - [x] 前台 / 管理端首页 200
- **备注 / 回滚**：
  - certbot 对 `kepu.printtfilm.com` NXDOMAIN、`www`/`admin.printfilm.com` 403 仍存在；旧证书文件仍可用
  - 已生成的科普成片需重新合成才会用新字体
  - 回滚：恢复 `/opt/ai_movie` 上一包并 `systemctl restart ai-movie-api`

## 2026-08-20 14:44 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（第二次重跑包含本地前端构建）
- **范围**：frontend / admin / api / nginx / env
- **变更摘要**：
  - 上线分镜页改动：删除“导入分镜板（多宫格）”、尾帧衔接默认关闭并支持页面开关
  - `全部生成` 改为跳过未改动且已生成的分镜；改过文案、时长、素材或封面图的分镜仍会重新生成
  - 清理前端一个未使用导入，修复生产构建报错后重新全量发布
- **验证**：
  - [x] `http://127.0.0.1:8000/api/health` 200（poller=`running`）
  - [x] `Host: kepu.printtfilm.com` 前台首页 200，`Last-Modified: Thu, 20 Aug 2026 06:42:31 GMT`
  - [x] `Host: admin.kepu.printfilm.com` 后台首页 200，`Last-Modified: Thu, 20 Aug 2026 06:42:36 GMT`
  - [x] `Host: kepu.printtfilm.com` `/api/health` 200
  - [x] `ai-movie-api` active；`ai-movie-worker` inactive；PG/Redis healthy
- **备注 / 回滚**：
  - 首次执行误继承 `SKIP_LOCAL_BUILD=1`，已发现并立刻重跑，最终结果以前端本地构建产物为准
  - certbot 对 `kepu.printtfilm.com` 仍因 NXDOMAIN 失败；`www.printfilm.com` / `admin.printfilm.com` 续签仍返回 403，但服务器上旧证书文件仍存在并继续使用
  - 回滚：恢复 `/opt/ai_movie` 上一包并 `systemctl restart ai-movie-api`

## 2026-08-20 13:44 · 绑定 www/admin.printfilm.com HTTPS

- **操作者**：Agent（按用户要求）
- **方式**：杭州 `8.136.41.61` 宝塔 nginx 热修
- **范围**：nginx vhost / CORS / 证书
- **变更摘要**：
  - 新增 `www.printfilm.com`、`admin.printfilm.com` 站点（前台 / 管理端，含 `/api` 与 `/epay/notify`）
  - 去掉 `kepu.printtfilm.com` 配置里抢占的正式域名，消除 80 端口 server_name 冲突
  - CORS 增加 `https://www.printfilm.com`、`https://admin.printfilm.com`
- **验证**：
  - [x] `https://www.printfilm.com/` 200，证书 CN=`www.printfilm.com`（至 2026-09-28）
  - [x] `https://admin.printfilm.com/` 200，证书 CN=`admin.printfilm.com`（至 2026-11-15）
  - [x] 两域名 `/api/health` 200
- **备注 / 回滚**：
  - Let's Encrypt 对本机 HTTP-01 返回 403（二次校验），未能在杭州新签；证书从旧机已有 live 目录拷入 nginx
  - 本机 certbot 无法自动续期这两张证书；到期前需放行 ACME 或改 DNS 校验
  - 回滚：删除对应 vhost 后 `nginx -s reload`

## 2026-08-20 13:33 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_LOCAL_BUILD=1` + `SKIP_BOOTSTRAP=1`）
- **范围**：frontend / admin / api / worker / nginx（宝塔 vhost）/ env
- **变更摘要**：
  - 同步当日管理端：模板网格/分类筛选、全局弹窗组件、系统设置（OSS/支付/从 .env 导入）、任务中心详情
  - 同步任务平台：分镜逐条 submit→poll、用户并发、序列批次失败联动
  - 上传改走本机 scp（约 42MB / 18s）
- **验证**：
  - [x] `http://127.0.0.1:8000/api/health` ok（`ark_mock=false`，poller running）
  - [x] `Host: kepu.printtfilm.com` 前台首页 200
  - [x] `Host: kepu.printtfilm.com` `/api/health` 200
  - [x] `Host: admin.kepu.printtfilm.com` 后台首页 200
  - [x] `ai-movie-api` / `ai-movie-worker` active；PG/Redis 容器 healthy
- **备注 / 回滚**：
  - OSS 北京内网探测超时，上传 endpoint 回落公网
  - nginx 提示 `kepu.printfilm.com` / `admin.kepu.printfilm.com` 80 端口 server_name 冲突（正式 HTTPS vhost 仍在宝塔原配置）
  - 脚本仍会写 `kepu.printtfilm.com` vhost；`printtfilm` 无 DNS，certbot 失败可忽略
  - 回滚：恢复 `/opt/ai_movie` 上一包并 `systemctl restart ai-movie-api ai-movie-worker`
