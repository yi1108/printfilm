# 发布记录 · 2026-09-03

## 2026-09-03 19:05 · 管理端隐藏剩余额度；杭州全量

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：admin
- **提交**：`a98d2db`
- **变更摘要**：用户列表/详情去掉「剩余额度」展示（业务未用 `quota_left`）；库列保留不 DROP
- **验证**：
  - [x] 脚本 `DONE`；`ai-movie-api` active；admin 首页 200

## 2026-09-03 19:01 · 分镜收尾幂等 + 去掉无限额度；杭州全量

- **操作者**：Agent
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（`SKIP_BOOTSTRAP=1`）
- **范围**：frontend / admin / api
- **提交**：`47fa9b3`（含此前 `b114a19` quota_left 启动补列）
- **变更摘要**：
  - 分镜视频 poll 收尾：6h 认领窗口、成片已落盘只补 complete、行锁后先 settle 再激活后续，避免并发双结算/双 complete
  - Seedance 用量按 `task_run+key` / `provider_task_id` 幂等；管理端统计仅折叠 seedance/seedream 重复行
  - 移除用户级 `billing_unlimited`（模型/API/管理端/用户端）；`skipped` 仅保留全局关计费
- **验证**：
  - [x] 脚本 `DONE`（约 0.9 min）；`ai-movie-api` active；poller running
  - [x] `Host: www.printfilm.com` `/api/health` 200；首页 200（`Last-Modified: Thu, 03 Sep 2026 11:00:59 GMT`）
  - [x] `Host: admin.printfilm.com` 首页 200
- **备注**：DB 列 `billing_unlimited` 有意未 DROP；原无限额账号发版后按钱包扣费

## 2026-09-03 · 主站切 www.printfilm.com（文档 / 发布脚本）

- **操作者**：Agent
- **方式**：更新 `deploy_kepu_8136.py` 验收 Host + 热修线上 `PUBLIC_BASE_URL`；文档同步
- **范围**：deploy 脚本（gitignore）/ docs / `PUBLIC_BASE_URL`
- **变更摘要**：
  - 发布验收与 DONE 输出改为 `www.printfilm.com` / `admin.printfilm.com`
  - `PUBLIC_BASE_URL`、易支付回调主站固定 www；kepu / printtfilm 仅兼容保留
- **验证**：
  - [x] `Host: www.printfilm.com` `/api/health` 200；首页 200
  - [x] 线上 `PUBLIC_BASE_URL=https://www.printfilm.com`；`EPAY_NOTIFY_URL` 已是 www
  - [x] kepu Host 仍 200（保留）
