# 发布记录 · 2026-08-24

## 2026-08-24 19:08 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户反馈全片合成失败）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量
- **范围**：frontend / admin / api
- **变更摘要**：
  - 浏览器无损拼接遇 HEVC 参数不一致（hvcC）时，自动回退服务端 ffmpeg：统一缩放到分集画幅后 H.264 拼接下载
  - 新增 `POST /api/drama/episodes/{id}/compose`
- **验证**：
  - [x] 脚本 `DONE`；health 200；首页 200
  - [ ] 业务：分集页再点「全片合成下载」，编码不一致时应自动走「服务端统一重编码拼接」
- **备注 / 回滚**：
  - 未改 nginx / 证书 / 中间件容器

## 2026-08-24 18:59 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求全量发布）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - i2v 有目标画幅时改用 `reference_image` + 显式 `ratio`（避免 first_frame 静帧竖屏仍吐横屏）
  - 生成队列：进行中点详情只看进度，不再挖历史「跳过重复任务」当当前失败
- **验证**：
  - [x] 脚本 `DONE`（约 0.9 min）；`ai-movie-api` active；health 200；poller running
  - [x] Host `kepu.printtfilm.com` / `admin.kepu.printtfilm.com` 首页 200（`Last-Modified: Mon, 24 Aug 2026 10:58 GMT`）
  - [ ] 业务冒烟：片段 18 需再重新生成确认竖屏
- **备注 / 回滚**：
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-24 18:38 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求全量发布）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 修复分镜视频「UI 显示 9:16 却出横屏」：i2v 兜底静帧按目标画幅生成（Seedance 首帧模式禁止传 ratio，会跟首帧比例）
  - Seedance `resolve_seedance_ratio` 缺省从 `16:9` 改为与漫剧一致的 `9:16`
- **验证**：
  - [x] 脚本 `DONE`（约 1.6 min）；`ai-movie-api` active；health 200；poller running
  - [x] Host `kepu.printtfilm.com` / `admin.kepu.printtfilm.com` 首页 200
  - [ ] 业务冒烟：需对已横屏的片段（如《神凡殊途定乾坤》片段 06/18）重新生成后确认竖屏
- **备注 / 回滚**：
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 已生成的横屏成片不会自动纠正，须重新生成
  - 回滚上一包并 `systemctl restart ai-movie-api`

## 2026-08-24 16:36 · 杭州 8.136.41.61 全量

- **操作者**：Agent（按用户要求全量发布）
- **方式**：`deploy/scripts/deploy_kepu_8136.py` 全量（未设 SETUP_NGINX / SETUP_TLS / SETUP_INFRA）
- **范围**：frontend / admin / api
- **变更摘要**：
  - 分镜「重新生成」不再因旧成片被调度器当成重复任务取消（`replace_existing_video` + queued 与任务同事务提交 + 孤儿 queued 宽限）
  - 同步本机未提交包：分集输出规格、客户端成片拼接、分镜页相关前端改动
- **验证**：
  - [x] 脚本 `DONE`（约 1.5 min）；`ai-movie-api` active；health 200；poller running
  - [x] 本机 `127.0.0.1:8000/api/health` 200
  - [x] Host `kepu.printtfilm.com` / `admin.kepu.printtfilm.com` 首页 200（`Last-Modified: Mon, 24 Aug 2026 08:34–08:35 GMT`）
- **备注 / 回滚**：
  - 未改 nginx / 证书 / Postgres·Redis 容器
  - 远端 OSS 内网探测超时，脚本改走公网 endpoint（与既有行为一致）
  - 回滚上一包并 `systemctl restart ai-movie-api`
